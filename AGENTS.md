# Project instructions

Read `docs/PROJECT_STATE.md` before work at every stage. The user's numbered prompt defines the scope; do not advance automatically.

Do not fabricate solver outputs, properties, references, convergence, validation, sensitivity, uncertainty or optimization results. Quantitative results require genuine solver output, a reproducible numerical calculation, or a verified publication with exact location. Design choices must be identified as choices, not findings.

Keep raw evidence immutable. Record input, script and output hashes. Failed runs remain traceable. Simulation repetitions are not independent physical experiments. Validation data must be independent of calibration.

Maintain the full manuscript, audit trail and project state. Rebuild the PDF, run relevant integrity checks, inspect every rendered page, commit stage changes, push and verify the remote commit. Do not deliver DOCX before Prompt 20. The sole requested stage deliverable is `Constrained-Annealing-2026-DRAFT.pdf`.
