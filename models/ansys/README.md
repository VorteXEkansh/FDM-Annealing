# ANSYS model status

No ANSYS model or result exists at Stage 1. Do not interpret this directory as evidence of a solver run.

Before execution record version/license availability, geometry and coordinate system, unit system, property sources and interpolation, constitutive history, initial state, thermal loads, contact settings, stabilization, solver settings, mesh and time integration. Archive input/output hashes and full solver logs under an immutable case ID. Binary outputs are excluded from ordinary Git by default; a later evidence-storage decision must preserve retrievability and checksums.
