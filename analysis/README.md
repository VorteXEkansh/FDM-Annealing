# Analysis boundary

Postprocessing accepts only non-empty solver exports with an explicit case,
time, quantity, unit, location, and source solver file. Derived responses will
be added only with a versioned equation/script and hashes of every input.

